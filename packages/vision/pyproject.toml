[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rok_vision"
version = "0.0.0"
description = "Placeholder for vision helpers package"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/vision"]

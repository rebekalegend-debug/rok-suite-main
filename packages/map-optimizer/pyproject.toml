[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rok_map_optimizer"
version = "0.0.0"
description = "Placeholder for map optimizer package"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/mapopt"]
